<h1>ジャンル編集</h1>

<div>
  <%= form_with model: @genre, url: admin_genre_path(@genre.id), method: :patch do |f| %>
  <div class="field">
    <label>ジャンル名</label>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit "変更を保存" %>
  </div>
  <% end %>
</div>