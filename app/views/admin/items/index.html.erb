
<div class="flex">
<h4>商品一覧</h4>

<div class="right">
  <%= link_to(new_admin_item_path, class: 'shadow') do %>
    <i class="fa-regular fa-plus maru"></i>
  <% end %>
</div>
</div>

<table class="bottom-border center space_30">
  <thead>
    <tr>
      <th>商品ID</th>
      <th>商品名</th>
      <th>税抜き価格</th>
      <th>ジャンル</th>
      <th>ステータス</th>
    </tr>
  </thead>

  <% @items.each do |item| %>
  <tbody>
    <tr>
      <td><%= item.id %></td>
      <td><%= link_to item.name, admin_item_path(item.id) %></td>
      <td><%= item.price.to_s(:delimited, delimiter: ",") %></td>
      <td><%= item.genre.name %></td>
      <td>
        <% if item.is_active %>
          <div class="true">販売中</div>
        <% else %>
          <div class="false">販売停止中</div>
        <% end %>
      </td>
    </tr>
  </tbody>
  <% end %>

</table>

<div class="width100">
  <div class="page">
    <%= paginate @items %>
  </div>
</div>